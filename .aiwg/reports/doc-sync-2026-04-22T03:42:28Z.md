# Documentation Drift Report
Generated: 2026-04-22T03:42:28Z
Direction: code-to-docs
Scope: full repo

## Summary

| Severity | Count | Auto-fixable |
|----------|-------|--------------|
| HIGH     | 32    | 10           |
| MEDIUM   | 28    | 0            |
| LOW      | 10    | 0            |
| **Total**| **70**| **10**       |

---

## HIGH Severity Findings

### CI Domain

- **[BROKEN-EXAMPLE]** `CLAUDE.md:106,112` — `act_runner exec -j ui-quality-checks` references non-existent job; actual job name in `.gitea/workflows/ui-ci.yml` is `quality-gate`
  Auto-fix: rename job in both CLAUDE.md command references ✓

- **[STALE]** `README.md:98,173` — Documents test command as `gh act -j frontend-tests` (GitHub Actions syntax, wrong job name). Should be `act_runner exec -j quality-gate -W .gitea/workflows/ui-ci.yml`
  Manual fix required (README may not exist or needs broader review)

- **[UNDOCUMENTED]** `.gitea/workflows/desktop-build-matrix.yml` — Rolling dev builds (Linux AppImage/deb + macOS DMG, published to `dev-latest`) completely undocumented
  Manual fix: add CI pipeline docs

- **[UNDOCUMENTED]** `.gitea/workflows/publish-dist.yml` — Publishes `hotm-ui-dist.tar.gz` to `hotm-latest` rolling release, consumed by BT6-ARSENAL. No docs exist.
  Manual fix: add to operator guide or CI docs

- **[UNDOCUMENTED]** `.gitea/workflows/publish-hotm-ui-image.yml` — Builds and pushes Docker image. Docker deployment referenced in README but build/tagging (latest, sha-\<7char\>, \<version\>) undocumented.
  Manual fix: add CI docs

- **[UNDOCUMENTED]** `.gitea/workflows/desktop-release.yml` — Tag-triggered versioned release builds undocumented. Release SOP references only manual `npm run build`.
  Manual fix: update release SOP

- **[UNDOCUMENTED]** `.gitea/workflows/mutsu-verify.yml` — SSH hybrid Mac runner (mutsu, 10.0.42.41) completely undocumented. No setup or maintenance docs.
  Manual fix: add ops documentation

- **[PARAMETER-MISMATCH]** `CLAUDE.md:106 vs docs/implementation/development-guide.md:25-27` — development-guide.md omits `test:coverage` and `test:realtime` scripts actually run in CI
  Manual fix: update development guide

### Architecture Domain

- **[STALE]** `CLAUDE.md:165` — Documents `ui/src/pages/` directory but it does not exist in the codebase
  Auto-fix: remove from project structure ✓

- **[STALE]** `CLAUDE.md:168` — Documents `ui/src/styles/` directory but it does not exist (CSS files are in `ui/src/` root)
  Auto-fix: remove from project structure ✓

- **[STALE]** `CLAUDE.md:173` — Documents `.github/` for CI/CD but project uses `.gitea/` (Gitea act_runner)
  Auto-fix: correct to `.gitea/` ✓

- **[UNDOCUMENTED]** `ui/src/services/` — Directory with WebSocket, upload, job event management (`websocket.ts`, `uploadStore.ts`, `jobEventStore.ts`, `realtimeEventBus.ts`) not in CLAUDE.md project structure
  Auto-fix: add to project structure ✓

- **[STALE]** `.aiwg/architecture/software-architecture-doc.md` — 1938-line SAD describes non-existent Axum/Rust backend, PostgreSQL, NLP pipeline. Hardcoded user path `/home/manitcor/` instead of `/home/roctinam/`. Describes architecture that was replaced by SPA-consuming-external-API model.
  Manual fix: SAD rebuild required (scope too large for auto-fix)

- **[PARAMETER-MISMATCH]** `.aiwg/architecture/software-architecture-doc.md:6` — SAD describes Axum server architecture; actual system is pure React SPA consuming external Fortemi API
  Manual fix: part of SAD rebuild

### Config Domain

- **[STALE]** `CLAUDE.md:103` — Documents `npm run lint` but no `lint` script exists in `ui/package.json`
  Auto-fix: remove from Development Commands ✓

- **[STALE]** `CLAUDE.md:204-206` — Documents `VITE_API_TIMEOUT` and `VITE_APP_TITLE` env vars that are not used anywhere in the codebase
  Auto-fix: remove from Environment Variables section ✓

- **[UNDOCUMENTED]** `ui/src/services/websocket.ts:75` — `VITE_DISABLE_WEBSOCKET` used in code, not in CLAUDE.md env vars
  Auto-fix: add to Environment Variables section ✓

- **[UNDOCUMENTED]** `ui/src/lib/sentry.ts:26` — `VITE_SENTRY_DSN` used for optional Sentry error tracking, not documented
  Auto-fix: add to Environment Variables section ✓

- **[UNDOCUMENTED]** `ui/src/components/HallOfMind.tsx:280` — `VITE_ENABLE_REALTIME_INSPECTOR` used in dev mode, not documented
  Auto-fix: add to Environment Variables section ✓

- **[PARAMETER-MISMATCH]** `ui/src-tauri/tauri.conf.json:4` — version `2026.2.0` does not match `ui/package.json` version `2026.2.18`
  Auto-fix: update tauri.conf.json to `2026.2.18` ✓

### API Domain

- **[STALE]** `docs/specifications/api-specification.md:157` — Documents `PUT /notes/{id}/status`; code uses `PATCH /notes/{noteId}/status`
  Manual fix: update API spec HTTP method

- **[STALE]** `docs/specifications/api-specification.md:174` — Documents `PUT /notes/{id}/revised`; code uses `PATCH /notes/{noteId}` via `updateRevision()`
  Manual fix: update API spec endpoint path

- **[UNDOCUMENTED]** `ui/src/api/extended.ts:229` — Code calls `POST /notes/{noteId}/reprocess` but docs say `POST /notes/{id}/regenerate-ai`
  Manual fix: verify against Fortemi API then update spec

- **[PARAMETER-MISMATCH]** `docs/specifications/api-specification.md:233` — Docs show response key `notes`; code expects `results` in `SearchResponse` type
  Manual fix: update API spec response schema

### Desktop Domain

- **[UNDOCUMENTED]** `ui/src-tauri/src/lib.rs:50-105` — `hotm_fetch` Tauri command proxies HTTP requests via reqwest backend. Not documented.
  Manual fix: add to operator guide

- **[UNDOCUMENTED]** `ui/src-tauri/src/lib.rs:110-178` — `hotm_sse_connect` implements SSE streaming via postMessage. Contract and lifecycle undocumented.
  Manual fix: add to operator guide

- **[UNDOCUMENTED]** `ui/src-tauri/src/lib.rs:358-363` — Global shortcut `CmdOrCtrl+Alt+H` toggles window visibility. Not in any user-facing docs.
  Manual fix: add to quick-start or operator guide

- **[UNDOCUMENTED]** `ui/src-tauri/src/lib.rs:305-307` — `--minimized` CLI flag for launch. Not documented in installation guides.
  Manual fix: add to installation docs

- **[UNDOCUMENTED]** `ui/src-tauri/src/lib.rs:233-243` / `ui/src/lib/tauri.ts:302-312` — `render_plantuml`/`ensure_plantuml` require external PlantUML server on `localhost:8080`. Zero setup documentation.
  Manual fix: add to operator guide (requires PlantUML server section)

---

## MEDIUM Severity Findings (manual review required)

- API: Labels implemented as tags (using `/notes/{noteId}/tags`) but docs describe separate `/labels` endpoints
- API: `/jobs/pending` vs documented `/jobs/queue`; jobs pause/resume API undocumented
- API: Archives, Chat, Webhooks, Events (SSE), Inference config, Federated search APIs all undocumented in api-specification.md
- API: `filter` query param mismatch (docs: single enum; code: separate `starred`/`archived` booleans)
- Desktop: Package name inconsistency in uninstall docs (`hot-m` vs `hotm`)
- Desktop: AppConfig fallback path differs between docs and code
- Desktop: Tray menu structure undocumented; close-to-tray behavior only partially covered
- CI: `quality-gate` job purpose undocumented in `docs/`; release SOP doesn't reference CI gates
- CI: `tauri-build.yml` Linux-only CI rationale undocumented
- CI: `tauri-build-cross.yml` manual cross-platform build process undocumented
- CI: `sdlc-gates.yml` PR template requirements undocumented
- CI: `post-deploy-validation.yml` smoke/UAT test trigger process undocumented
- Config: `TAURI_DEV_HOST` and `VITE_GIT_SHA` used in `vite.config.ts` but undocumented
- Config: `.env` vs `.env.local` inconsistency between CLAUDE.md and development-guide.md
- Architecture: `docs/architecture/system-architecture.md` 15-line stub contradicts detailed SAD

## LOW Severity Findings (informational)

- Desktop: `invokeTauri()` wrapper undocumented (internal implementation detail)
- Desktop: `useBlobUrl()` hook undocumented
- Desktop: `hotm_sse_close` no-op placeholder undocumented
- Desktop: PlantUML version references `2026.2.0` not `2026.2.18` in some install docs
- CI: `docs-link-check.yml` lychee validation undocumented (internal CI detail)
- Architecture: `ui/src/api/` has 30+ modules; CLAUDE.md API list covers only 6
- Config: Runtime `window.__RUNTIME_CONFIG__` Docker injection pattern differs from documented `.env` approach
- Config: `.env` vs `.env.local` naming inconsistency (minor)
- API: Metadata labels API merging labels into tags (undocumented design decision)
- API: Job queue naming inconsistency (informational)

---

## Files Modified by Auto-fix

| File | Changes |
|------|---------|
| `CLAUDE.md` | CI job name, project structure, env vars, lint script |
| `ui/src-tauri/tauri.conf.json` | Version sync to 2026.2.18 |

---

## Items Requiring Manual Review (prioritized)

1. **SAD rebuild** — `.aiwg/architecture/software-architecture-doc.md` documents a non-existent Axum backend. Needs complete rewrite to reflect SPA + external Fortemi API model.
2. **API spec updates** — `docs/specifications/api-specification.md` has 4 HIGH findings + 10 MEDIUM gaps (missing modules: archives, chat, webhooks, events, inference, federated search).
3. **PlantUML docs** — Document PlantUML server requirement and setup in operator guide.
4. **Desktop keyboard shortcut docs** — `CmdOrCtrl+Alt+H` and `--minimized` flag need user-facing documentation.
5. **CI pipeline docs** — 7 undocumented workflows need a CI overview document.
6. **Release SOP** — Update to reference automated tag-triggered desktop builds.
