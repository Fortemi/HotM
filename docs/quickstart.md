# Quickstart (Legacy Alias)

Use `docs/quick-start.md` for the current client-only setup.

Legacy desktop-era instructions were removed from active docs and are retained only in historical archives under `.aiwg/archive/desktop-era/`.
