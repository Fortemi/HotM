# HotM 2026.5.0

**Release date:** 2026-05-06
**Type:** Minor — version-aligned with Fortemi v2026.5.0; no HotM API contract changes

This release bumps HotM to track the matching Fortemi minor (`v2026.5.0` — *Filesystem attachment durability + CI Docker fix*). Bundled desktop builds and the Docker UI image now pair with the latest Fortemi sidecar by default. The HotM-side changes since v2026.4.1 are limited to host-adapter compatibility, macOS install polish, CI fixes, and an AIWG framework refresh.

## What's changed

### 🔗 Version alignment with Fortemi v2026.5.0

HotM and Fortemi share a release cadence; v2026.4.1 → v2026.5.0 is the catch-up to Fortemi's minor bump. The Fortemi sidecar shipped in this release contains:

- Filesystem attachment durability (Fortemi-side)
- CI Docker pipeline fix (Fortemi-side)

See the [Fortemi v2026.5.0 release](https://git.integrolabs.net/Fortemi/fortemi/releases/tag/v2026.5.0) for details. Nothing on the HotM API contract changed.

### 🛠 `__HOTM_HOST__` adapter contract v1 (#192)

The host-adapter now accepts an optional `version: number` field so embedders can declare the contract revision they implement. Adapter shape is unchanged; absent `version` is treated as v0. See [`docs/host-adapter.md`](../host-adapter.md) for the full contract.

### 🍎 macOS install polish

- `setup-macos.sh` is now at full parity with `setup-linux.sh` (sidecar staging, `.env` handling, Ollama detection, port allocation).
- The install docs and post-install script walk users through the **System Settings → Privacy & Security** Gatekeeper approval needed for the self-signed `.app` bundle on first launch.

### 🤖 AIWG framework refreshed (internal)

AIWG bumped to 2026.4.0-rc.31. The Codex provider deployment is dropped from this repo's AIWG configuration; Claude Code remains the supported provider.

### 🔧 CI

`ui-ci.yml` checkout aligned with the Fortemi workflow pattern (#191), eliminating an intermittent `${GITHUB_SERVER_URL}` resolution failure that surfaced under local `act_runner` execution.

## Upgrade notes

Drop-in replacement on every platform — no migration required.

### Linux (.deb / AppImage)

```bash
sudo apt-get install ./HotM_2026.5.0_amd64.deb
```

### macOS

DMG drop-in replace. If this is your first install of a self-signed HotM build, follow the Gatekeeper approval flow in [`docs/installation/macos.md`](../installation/macos.md).

### Windows

NSIS installer drop-in replace.

### Docker UI image

```bash
docker pull git.integrolabs.net/fortemi/hotm-ui:2026.5.0
```

Pair with `git.integrolabs.net/fortemi/matric-api:2026.5.0` for the matched-version stack.

## Compatibility

| Component | Version |
|---|---|
| Fortemi (matric-api) | **2026.5.0** (matched) |
| Earlier Fortemi versions | 2026.4.x — supported (no contract changes) |
| Host adapter contract | v1 (`version` field optional, backward-compatible) |

## Commit references

- `cdcdfd1` — `chore(aiwg): refresh framework to 2026.4.0-rc.31, drop codex provider`
- `a1feca4` — `feat(host-adapter): contract v1 with optional version field + docs (#192)`
- `e200b43` — `ci: align ui-ci.yml checkout with fortemi pattern (#191)`
- `cbbc988` — `docs(macos): hand-hold Gatekeeper approval for self-signed bundle`
- `94fe2cc` — `fix(macos): bring setup-macos.sh + docs to parity with desktop-linux`
- (this release) — version bump to 2026.5.0
